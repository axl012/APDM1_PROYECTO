<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hipo.prop.app.axel.com.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Menu"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Ticket" />
        <activity android:name=".TacosList" />
        <activity android:name=".ListView" />
        <activity android:name=".Detail" />
        <activity android:name=".TortasList" />
        <activity android:name=".DrinksList" />
        <activity android:name=".PostresList" />
        <activity android:name=".TicketView"></activity>
    </application>

</manifest>